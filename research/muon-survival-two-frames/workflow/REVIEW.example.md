# Gate review: gate name, version 1

- Reviewer actor (self-asserted process ID):
- Coordinator confirmation that this is a different session/person from the
  submitter:
- Submitted event number:
- Exact evidence snapshot paths and SHA-256 values reviewed:
- Checks reproduced:
- Protocol, code, run, analysis, or prose findings:
- Confirmatory versus exploratory boundary checked:
- Blocking findings:
- Nonblocking notes:
- Decision and graph route:
- Frozen resume/retry/rerun authorization checked (when applicable):
- Candidate content commit, cumulative attestation-only delta, and final-CI
  condition (PR review only):
- Why that route is appropriate:

The reviewer does not edit the submitted artifact. A different actor-ID string
does not by itself prove independence. Save this small report under a new
versioned `workflow/` name; the coordinator records the graph transition. Use
repository-relative paths or durable external identifiers, never local absolute
filesystem paths.
