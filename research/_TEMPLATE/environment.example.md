# Environment

Use an ecosystem-native lockfile whenever possible. If the experiment cannot be
fully locked, record the remaining execution boundary here.

- Operating system and version:
- Architecture:
- Interpreter/compiler and version:
- Dependency manager and version:
- Hardware assumptions:
- Locale/timezone:
- Random seeds and nondeterministic operations:
- Required environment variables (names only; never commit values or secrets):
- External services, models, and dated version identifiers:
