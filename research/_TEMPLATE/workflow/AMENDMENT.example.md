# Post-exposure protocol amendment, version 1

- Actor and role (self-asserted process ID):
- Originating review event and `amend` decision:
- Exposed results and evidence snapshots:
- Invalid or incomplete lineage:
- Quarantined runs, analyses, metrics, figures, and prose:
- Old protocol path and SHA-256:
- Proposed protocol path and SHA-256:
- Exact changed decisions:
- Exact unchanged decisions:
- Confirmation-bias risk created by exposure:
- Controls that keep the revision interpretable:
- Why this still answers the registered question:
- Why this is not a new hypothesis or workflow:
- Required new amended-setup, execution, and analysis artifacts:
- Journal checkpoint and explicit next step:

Keep this receipt small under `research/<experiment-slug>/workflow/`. Use only
repository-relative paths or durable external identifiers; never record local
absolute checkout, home, scratch, cache, or mounted-data paths. Do not edit the
frozen protocol or exposed lineage in place. `amendment_review` must approve
this packet before the new version enters `amended_setup` and
`amended_setup_review`.
